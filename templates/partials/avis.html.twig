
{% block stylesheets %}

  <style>
  
  </style>
{% endblock %}

<div class="block p-6 rounded-lg shadow-lg bg-brown " style="margin-bottom:10%">
    <div class="md:flex md:flex-row row justify-content-start">
    
        <div class="md:w-96  flex mb-6 lg:mb-0 col-4 mx-4 my-4 py-4" style="width:10%;padding-left:50px">
            
       
        </div> 
        <div class=" px-4 col-4 py-4">
          {# <p class="font-semibold text-xl mb-2 text-gray-800"><bold>Par:</bold> {{avis.user.prenomClient}} {{ avis.user.nomClient }}</p> #}
         

         {# Arranger design de ca  #}
          {# <p class="font-semibold text-xl mb-2 text-gray-800"><em> Date:</em></p> #}
         

         
          <div class="etoiles">
            {% for i in range(1, 5) %}
                {% if arrayAvis.notes >= i %}
                  <i class="bi bi-star-fill" style="color:yellow;"></i>
                {% else %}
                  <i class="bi bi-star" style="color:black"></i>
                {% endif %}
            {% endfor %}
          </div>
              <p class="text-gray-500 font-light mb-6">
                {{ arrayAvis.commentaire|raw }}
              </p>
            
        </div>
    </div>
</div>  